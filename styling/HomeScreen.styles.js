import { StyleSheet } from 'react-native';

export default StyleSheet.create({
	container: {
		textAlign: 'center',
		fontWeight: 'bold',
		fontSize: 20,
		paddingTop: 20,
	},
	activityIndicator: {
		flex: 1,
		alignItems: 'center',
		justifyContent: 'center',
	},
});
